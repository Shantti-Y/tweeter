<% if log_user.following?(@user) %>
  <%= link_to 'Unfollow', follow_path(@user), method: :delete, class: 'btn btn-danger', remote: true %>
<% else %>
  <%= link_to 'Follow', follows_path(id: @user), method: :post, class: 'btn btn-info', remote: true %>
<% end %>
